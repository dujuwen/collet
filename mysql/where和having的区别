select * from [xx] where [xx] group by [xx] having [xx]

sql查询的关键字顺序为: from -> where -> group by-> having ->order by

1. where是对原始数据进行过滤，having是对分组后的数据进行过滤
2. where后面不能用聚合函数，having能
3. where后面不能用字段别名(where是最开始执行)，having和group by后面能用字段别名
