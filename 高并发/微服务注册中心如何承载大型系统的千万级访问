https://juejin.im/post/5be3f8dcf265da613a5382ca

核心:
1.纯内存操作，保证高性能
2.一定时间的间隔请求(30s)，减少核心服务器的请求次数
3.多级缓存机制，确保了不会针对内存数据结构发生频繁的读写并发冲突操作，进一步提升性能

只读缓存
写缓存
缓存

第一次：
缓存=写缓存=只读缓存

第二次:
缓存(更新)同时清空写缓存 只读缓存不变

30s后同步新的缓存:
只读缓存 <- 写缓存 -> 缓存

只读缓存发现写缓存未空，重新请求新的缓存
