https://testerhome.com/topics/8313

在不借助网络的情况下如何查找需要的命令？
可以使用的命令有：
man -k = apropos //它们是等效的
命令后面跟关键字
可以将它们看做是单机版的命令数据库关键字搜索工具

例如：如何获取生成uuid的命令？
apropos uuid | grep "(1)" //一般查看(1)类命令就可以了

一般是使用apropos或者man -k定位命令后结合过滤器查找需要的，最后使用man命令查看具体内容
